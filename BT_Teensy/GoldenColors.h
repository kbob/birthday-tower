#ifndef GOLDEN_COLORS_included
#define GOLDEN_COLORS_included

#include <stdint.h>

// Generate saturated colors using the golden ratio.

extern uint32_t golden_color_RGB(uint32_t index, uint16_t sat, uint16_t val);

#endif /* !GOLDEN_COLORS_included */
